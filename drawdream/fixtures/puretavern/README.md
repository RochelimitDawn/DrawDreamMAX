# PureTavern 兼容 Fixture

`manifest.json` 是兼容 fixture 的版本化索引。fixture ID 与 `src/tavern/compat/inventory.ts` 中的契约矩阵保持一致，测试使用现有 Agent fixture 和 contract test 覆盖这些行为。

PureTavern 只作为公开行为参考，当前实现使用 clean-room adapter。参考仓库 commit 和 AGPL-3.0 信息记录在 manifest 与 `docs/puretavern-attribution.md` 中。
